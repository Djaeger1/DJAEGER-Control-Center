name: HERMES WORK Legacy Cutover Bootstrap (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy channel retained as recovery fallback
        run: |
          echo "DJAEGER WORK automatic releases moved to Djaeger1/DJAEGER-WORK."
          echo "This legacy branch is retained only as a v2.5.1 repository-cutover recovery bootstrap."
          echo "Do not publish new DJAEGER WORK releases from this repository."
