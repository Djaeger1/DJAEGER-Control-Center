name: REBUILD2 SLIM SMOKE
on:
  push:
    branches: [codex/hermes-neuron-ask-rebuild2]
    paths: ['.github/workflows/rebuild2-slim-smoke.yml']
jobs:
  smoke:
    runs-on: ubuntu-slim
    steps:
      - run: |
          set -e
          echo SLIM_RUNNER_OK
          uname -a
