name: DJAEGER WORK Native Dashboard APK Legacy (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "DJAEGER WORK source and APK build moved to Djaeger1/DJAEGER-WORK."
          echo "This legacy workflow is retained only for audit history."
