plugins {
    id 'com.android.application'
}

android {
    namespace 'com.hermes.workdashboard'
    compileSdk 35

    signingConfigs {
        release {
            storeFile file('hermes_work_terminal.jks')
            storePassword 'HermesWork2026'
            keyAlias 'hermeswork'
            keyPassword 'HermesWork2026'
        }
    }

    defaultConfig {
        applicationId "com.hermes.workdashboard"
        minSdk 23
        targetSdk 35
        versionCode 120
        versionName "1.2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }
}
